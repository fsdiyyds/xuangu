akshare>=1.14.0; python_version >= "3.9"
requests>=2.28.0
pandas>=1.5.0
numpy>=1.23.0
scikit-learn>=1.3.0
lightgbm>=4.0.0
pyyaml>=6.0
matplotlib>=3.7.0
tabulate>=0.9.0

# LSTM / Qlib 时序模型（GRU / ALSTM / Transformer）
tensorflow>=2.10.0; python_version >= "3.8"
streamlit>=1.28.0
plotly>=5.18.0
scipy>=1.10.0
tqdm>=4.66.0

# Qlib 风格表格模型（可选，未安装则对应模型自动跳过）
xgboost>=2.0.0
catboost>=1.2.0

# Python 3.8 用户: akshare 不可用，脚本会自动使用东方财富直连 + requests
# 建议升级到 Python 3.10+ 以获得完整 akshare 支持
# 完整 Qlib 平台（可选，本项目已内置轻量 Zoo，不强制）:
#   pip install pyqlib
